#include <iostream>
#include <bitset>
#include <vector>
#include <cmath>
#include <fstream>
using namespace std;

namespace eliasCoding{
  void encode(int num, bool* bitCode);
  void decode(int* num, vector<bool> bitCode);
}
